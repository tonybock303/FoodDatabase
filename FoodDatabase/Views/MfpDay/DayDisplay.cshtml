@model FoodDatabase.Models.MyFitnessPalDay.MfpDay

@{
    ViewBag.Title = "DayDisplay";
}
@Html.Partial("_Calendar")
<br />
@if (Model.meal1.Count() > 0)
{

    @Html.Partial("_MealDisplay", Model.meal1)
}

@if (Model.meal2.Count() > 0)
{
    @Html.Partial("_MealDisplay", Model.meal2)

}
@if (Model.meal3.Count() > 0)
{
    @Html.Partial("_MealDisplay", Model.meal3)

}
@if (Model.meal4.Count() > 0)
{
    @Html.Partial("_MealDisplay", Model.meal4)

}
@if (Model.meal5.Count() > 0)
{
    @Html.Partial("_MealDisplay", Model.meal5)

}
@if (Model.meal6.Count() > 0)
{
    @Html.Partial("_MealDisplay", Model.meal6)
}
